@model List<User>
@for (int i = 0; i < Model.Count; i++)
{
    <span class="label label-success" style="margin: 2px; font-weight: 500;"> @Model[i].Name </span>
    if ((i+1)%6 == 0)
    {
        <p></p>
    }
}